const roiButton = document.getElementById('roi');
const roiSection = document.querySelector('.roi-section');

const simpleButton = document.getElementById('simple');
const simpleSection = document.querySelector('.simple-section');

const compoundButton = document.getElementById('compound');
const compoundSection = document.querySelector('.compound-section');

export {
  roiButton,
  roiSection,
  simpleButton,
  simpleSection,
  compoundButton,
  compoundSection,
}