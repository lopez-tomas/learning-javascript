// # O(n * log(N)) solution
var form = document.getElementById('anagram-form');
var firstWord = document.getElementById('first-word');
var secondWord = document.getElementById('second-word');
form.addEventListener('submit', function () {
});
// const sortWord(word) => {
// }
